/**
 * given a matrix, contains num on some pos (i, j), all non-negative
 * travel from left-up to right-bottom, then back to left-up,
 * return the max sum of this route. one cell could only been added on time.
 */
public class Solution {

    public int maxSum(int[][] matrix) {
        // dp[n][i][j] means after n steps the first route target at row i
        // and second route target at row j
        // int[][][] dp = new int[][][];
    }
}
