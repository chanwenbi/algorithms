/**
 * Find the subarray which sum equals to zero. O(n)
 *
 * idea:
 * keep prefix sum in a hashmap, key: sum, value: end pos
 * if found an exist key in hashmap, then return the region between this end pos
 */
public class Solution {

    public int[] subarrayEqualToZero(int[] nums) {

    }
}
