/**
 * Implement a queue by two stacks. Support O (1) push, pop, top.
 *
 * idea:
 * when push, push to stack1.
 * when pop, check whether stack2 is empty, if not pop from it.
 *           else pop all elements from stack1 to stack2
 */
public class Solution {

}
